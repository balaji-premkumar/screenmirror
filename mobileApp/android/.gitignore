# The Gradle wrapper is committed on purpose: CI and contributors must
# build with the exact Gradle version this project was verified against.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Native libraries are regenerated by the buildRust Gradle task on every
# build, so tracking them only dirties the tree and bloats history.
app/src/main/jniLibs/
